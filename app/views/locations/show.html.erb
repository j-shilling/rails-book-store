<h1><%= @location.name %></h1>
<p>Phone Number: <%= @location.phone_number %></p>

<p><%= link_to pluralize(@location.books.count, 'Book'), location_books_path(@location) %></p>
<p><%= link_to pluralize(@location.authors.count, 'Author'), location_authors_path(@location) %></p>

<%= link_to "Edit", edit_location_path(@location) %>
<%= link_to "All Locations", locations_path %>
